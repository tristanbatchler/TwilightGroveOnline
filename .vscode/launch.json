{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run server",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/server/cmd/main.go",
            "output": "${workspaceFolder}/server/cmd/debug_executable.exe",
            "args": [
                "--config", "${workspaceFolder}/server/.env"
            ],
            "cwd": "${workspaceFolder}/server",
            "preLaunchTask": "syncAll",
        }
    ]
}